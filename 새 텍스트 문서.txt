define의 장점
상수 쓸때는 장점으로 but 그이상은 ㄴㄴ

소켓 통신
	채팅예제
	한번씩 주고받는게 아닌 몇번씩 보내는게 가능해짐
	쓰레드 수신, 송신 둘다 하려면 멀티로
	메인이 쓰레드임
	메세지 왔다갔다가 아니라 클래스가 왔다갔다
	클래스의 크기를 최대한 작게 	


접근 지정자 왠만하면 private로 하자.
private 함수이름: 얘는 안에서 돌아가는
public 아 얘는 밖에서 도는 애구나

python에서는 _get_value 처럼 앞에 _붙으면 내부로 도는 애
또 python에서 for _ in range(10)  에서 _는 
나는 여기서만 쓰는 tmp 변수야


큰 함수를 만들고 내부적으로 동작하는 함수로 쪼개놓는 것도 좋은 방법
변수명 지키기, 언어의 암묵적인 룰 지키기, 

이중연결리스트 삽입,삭제 연산문제 나왔었는데
직접 그림을 그려가면서 풀어보자

스택 큐 덱 링크드리스트 구현해보기(시간나면)